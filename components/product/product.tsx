import SectionTitle from "../section-title";
import WebConf from "../webconf/webconf";

export default function Product() {
  return (
    <div>
      <SectionTitle title="Product" />
      <WebConf />
    </div>
  );
}
